добавить цвета блоков более оптимально? рандомайзер enum
выделение блока при нажатии


вопрос с сеткой: не видит линий


void CreateBlocksNew() {
        srand(time(0));
        CreateMatrix();
        Color blockColor;
        for (int i = 0; i < blocksInRow; i++) {
            for (int j = 0; j < blocksInRow; j++) {
                Block block((2 * i + 1) * (sqrt(blockSide * blockSide / 2) + 7), (2 * j + 1) * (sqrt(blockSide * blockSide / 2) + 5));
                blocksM[i][j] = block;
            }
        }
    }


      void CreateMatrix() {
        blocksM = new Block* [blocksInRow];
        for (int i = 0; i < blocksInRow; ++i) {
          //  Block* tmpBlock = new Block[blocksInRow]();
          //  blocksM[i] = new Block[blocksInRow];
        }
    }